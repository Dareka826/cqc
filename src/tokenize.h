#ifndef __TOKENIZE_H_jtrG0fhY
#define __TOKENIZE_H_jtrG0fhY

#include <stdlib.h>
#include <string.h>
#include "array.h"

arr_token tokenize(const char * const src) {
    arr_token result = { .len = 0, .ptr = NULL };
    return result;
}

#endif
